generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Configurations {
  id              String  @id @default(cuid()) //cuid - collision-resistant ids
  imgUrl          String
  width           Int
  height          Int
  croppedImageUrl String?
}
